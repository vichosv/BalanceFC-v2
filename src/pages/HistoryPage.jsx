export default function HistoryPage({ ctx }) {
  return (
    <div className="page">
      <div className="page-title">📊 Historial</div>
      <p style={{ color:'var(--muted)' }}>Coming soon — migrating from v1</p>
    </div>
  );
}
